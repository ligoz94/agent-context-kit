# Feature draft (repository root)

This file is **not** part of the agent-context-kit scaffold. The init template puts feature specs under **`docs/features/`** and registers them in **`manifest.yaml`**.

- **Quick scaffold:** `npx @agent-context-kit/cli new-spec <name>`
- **Template to copy:** `template/docs/features/_template.md` (or `docs/features/_template.md` after `init`)
- **Conventions:** see `template/docs/features/README.md`

Delete or replace this file in your own repo as you prefer.
